# DAON Agent System v1.0.0 — 지인 체험판 (슬림 빌드)

> 내부 테스트용 배포판입니다. **Windows 64비트(x64)** 전용.
> (최신 갱신: 2026-09-16)

## 다운로드할 파일

**`DAON Agent System Setup 1.0.0.exe`** (약 322 MB)

## 설치 방법

1. 위 파일을 다운로드합니다.
2. 파일을 더블클릭해서 실행합니다.
3. **"Windows의 PC 보호"** 경고가 뜨면 → **추가 정보** → **실행** 을 누릅니다.
   - 코드 서명(유료 인증서)이 없어서 뜨는 정상 경고입니다. 바이러스가 아닙니다.
4. 안내에 따라 설치를 완료합니다.
   - 시작 메뉴 / 바탕화면 바로가기가 자동으로 생성됩니다.

## 이번 빌드(2026-09-16)의 변경점

### 서버 재시작 폭풍 근본 수정
- **회로 차단기(circuit breaker)가 실제로 동작하도록 수정했습니다.**
  - 기존에는 crash 감지 창(15초)이 onefile 서버의 실제 종료 시간(약 60초)보다
    짧아, 매번 카운터가 0으로 리셋되어 8회 연속 실패 시 자동 중단 로직이
    **구조적으로 발동할 수 없었습니다.** 감지 창을 150초로 확대했습니다.
- **포트 충돌(EADDRINUSE)을 명시적으로 감지**해 급속 실패로 카운트합니다.
- **스왑/롤백 직후 유예 판정 보정** — onefile `_MEI` 추출(수십 초) 동안
  서버가 아직 포트를 열지 않아도 "이미 종료됨"으로 오판해 재기동을 중단하던
  문제를 수정했습니다. 롤백·백업 복원 재기동 경로에도 동일한 심층 유예창을
  적용했습니다.

### 실행 표면(surface) 도구 누수 수정
- WebUI에서 세션을 재사용할 때 이전 크롬 확장 표면이 남아
  브라우저 도구(`browser_*`)가 계속 제거되던 문제를 수정했습니다.
- 이제 WebUI는 항상 `webui` 표면을 선언하고, 세션에 표면 정보가 저장됩니다.

### 빌드/배포 안정화
- 미러(`dist_new`) 동기화 + 스펙 이중 검증 + 카나리 기동 검증 파이프라인을
  그대로 유지했습니다.
- 포터블 설치 스크립트의 경로 단일 정의 모듈 참조 버그를 수정했습니다.

## 이전 빌드의 주요 변경점 (참고)

- **용량 대폭 감소: 1.2 GB → 약 322 MB** — 설치 중 압축 해제가 멈춘 것처럼
  보이던 문제 완화.
- **기억 워커 토큰 누수 방지** — 기본값은 "무료 로컬 라우터(OmniRoute)가 감지될
  때만" 기억 자동 추출/정제를 켭니다. OmniRoute를 돌리지 않는 환경에서는 유료
  모델 토큰을 소모하지 않습니다. (기억 기능 자체는 정상 동작)
  - 앱을 껐다 켤 때마다 하루치 정제(LLM)가 즉시 한 번씩 돌던 **재시작 폭주
    버그**를 수정했습니다.
  - 원한다면 `config.yaml`의 `memory.auto_extract` 값을 직접 바꿔 항상
    켜거나(`"on"`) 항상 끌 수(`"off"`) 있습니다.

## 사용 참고

- 앱은 트레이에 상주하고, `server.exe`(Python 서버)가 백그라운드에서 함께 실행됩니다.
- **절전 모드(S3)** 에 들어가면 서버가 중단됩니다. 계속 돌리려면 Windows 전원 설정에서
  절전 모드를 "안 함"으로 두세요. (화면 꺼짐·화면 보호기는 서버를 멈추지 않습니다.)
- **AI 모델 API 키는 각자 본인 것을 앱 안에서 입력하면 됩니다.**
  배포 파일에는 어떤 키도 포함되어 있지 않습니다.
- 이전 버전이 이미 설치되어 있다면 이 버전으로 덮어 설치하면 됩니다.

## 문제가 있을 때

- 앱 로그: `%APPDATA%\daon-agent-system\daon-main.log`
- 설치 위치: `%LOCALAPPDATA%\Programs\daon-agent-system`
